_______FIRST WEEK________
git --version // We use it to determine the git version

git version 2.20 // result

git config --global user.name "Jamal raouj"
                  // We use it to give the username for git
git config --global user.email "jamalrwj@gmail.com" 
                 // We use it to give the email for git

git config --global init.defaultBranch main 
                //To change from master to main

git config --get user.name 
                // show username
git config --get user.email 
                // show email

ls ~/.ssh/id_rsa.pub 
                // Check if SSH

ls: cannot access '/home/votre-nom-utilisateur/.ssh/id_rsa.pub':
                
                //This message appears when ssh is not present 

ssh-keygen -C jamalrwj@gmail.com 
                //fetch key for ssh
cat ~/.ssh/id_rsa.pub
                // for show key

git clone 
             // git clone used to  take the repository from gitHub to local 

git remote -v //In order to check if  repository is online

              //Create a new file in git_test folder called README.md 
git status   
             // We use it to check if there has been a change in files
git add . 
            // To add changes or new files ,like README.md
git commit -m "add README.md" 
            //Description of changes
git push origin main 
            //push up the project

===============================
In the C programming language:
  Data type in C __ These variables we use to enter data, control it and change it in the
                   code according to the result we are looking for:

   	int take 2 bytes : //we use this variable to enter an integer :
		this variable in scanf("%d",&num);
	char take 1 byte : // we use this variable to entre a character :
		this variable in scanf("%c",&name_char);
	char[n] take n bytes : //we use this variable to entre text :
		this variable in scanf("%s",&name_char);
	float take 4 bytes : //we use this variable to enter a numbere real:
		this variable in scanf("%f",&num);

	double take 8 bytes : //we use this variable to enter a numbere real Such as float type but double has more memoiry :
		this variable in scanf("%lf",&num);
 
  printf("information var %d ",name_var);
  scanf("type data with % sush as %d or %c etc.." ,& name_var);

  if/else condition :
      we use if statement for check the condition :
      If the condition is met, the code will be executed :
      If it does not come true :
      Code is executed inside else :
      // for example :
     		 #include <stdio.h>
     		 int main (){
     		   printf("Entre one number :");
     		   int i ;
     		    scanf("%d",&i);
     		   if(i <=50){
     			 if(i>=0){
     			   printf("this number 0<= %d <=50",i);
 			  }
   
		 }else{
                       printf("this number is not 0<= number <=50");
            }

    return 0;

}
     //and loop for


  
  


  
	

	






